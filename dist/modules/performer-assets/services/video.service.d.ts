import { Model } from 'mongoose';
import { ObjectId } from 'mongodb';
import { QueueEventService, QueueEvent, AgendaService } from 'src/kernel';
import { FileDto } from 'src/modules/file';
import { FileService } from 'src/modules/file/services';
import { ReactionService } from 'src/modules/reaction/services/reaction.service';
import { PerformerService } from 'src/modules/performer/services';
import { SubscriptionService } from 'src/modules/subscription/services/subscription.service';
import { UserDto } from 'src/modules/user/dtos';
import { CheckPaymentService } from 'src/modules/payment/services';
import { VideoUpdatePayload } from '../payloads';
import { VideoDto } from '../dtos';
import { VideoCreatePayload } from '../payloads/video-create.payload';
import { VideoModel } from '../models';
export declare const PERFORMER_VIDEO_CHANNEL = "PERFORMER_VIDEO_CHANNEL";
export declare const PERFORMER_VIDEO_TEASER_CHANNEL = "PERFORMER_VIDEO_TEASER_CHANNEL";
export declare const PERFORMER_COUNT_VIDEO_CHANNEL = "PERFORMER_COUNT_VIDEO_CHANNEL";
export declare class VideoService {
    private readonly performerService;
    private readonly reactionService;
    private readonly checkPaymentService;
    private readonly subscriptionService;
    private readonly PerformerVideoModel;
    private readonly queueEventService;
    private readonly fileService;
    private readonly agenda;
    constructor(performerService: PerformerService, reactionService: ReactionService, checkPaymentService: CheckPaymentService, subscriptionService: SubscriptionService, PerformerVideoModel: Model<VideoModel>, queueEventService: QueueEventService, fileService: FileService, agenda: AgendaService);
    private defineJobs;
    private scheduleVideo;
    findById(id: string | ObjectId): Promise<VideoDto>;
    findByIds(ids: string[] | ObjectId[]): Promise<VideoModel[]>;
    getVideoForView(fileDto: FileDto, video: VideoDto, jwToken: string): {
        width: number;
        height: number;
        url: string;
        duration: number;
        thumbnails: string[];
    };
    handleTeaserProcessed(event: QueueEvent): Promise<void>;
    handleFileProcessed(event: QueueEvent): Promise<void>;
    create(video: FileDto, teaser: FileDto, thumbnail: FileDto, payload: VideoCreatePayload, creator?: UserDto): Promise<VideoDto>;
    getDetails(videoId: string, jwToken: string): Promise<VideoDto>;
    userGetDetails(videoId: string, currentUser: UserDto, jwToken: string): Promise<VideoDto>;
    updateInfo(id: string | ObjectId, payload: VideoUpdatePayload, files: any, updater: UserDto): Promise<VideoDto>;
    delete(id: string | ObjectId): Promise<boolean>;
    increaseView(id: string | ObjectId): Promise<any>;
    increaseComment(id: string | ObjectId, num?: number): Promise<any>;
    increaseLike(id: string | ObjectId, num?: number): Promise<any>;
    increaseFavourite(id: string | ObjectId, num?: number): Promise<any>;
    increaseWishlist(id: string | ObjectId, num?: number): Promise<any>;
    checkAuth(req: any, user: UserDto): Promise<boolean>;
}
